#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void print_alphabet_x10(void)
{
        int x = 0;
	int y;

        while (; x < 11; x++)
        {
                _putchar('\n');
		while (y = 'a'; y <= 'z'; y++)
		{
			_putchar(y);
		}
	}
	return (0);
}
