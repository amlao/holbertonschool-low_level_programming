#include "holberton.h"

/**
 *print_sign(int n) - write a function that prints the sign of a number
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c >= 'z') || (c >= 'A' && c <= 'Z'))
	{
		return (1);
	}
		return (0);
}
