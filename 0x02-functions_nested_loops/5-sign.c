#include "holberton.h"

int print_sign(void)
{
	int x;

	if (x > 0)
	{
		_putchar(1);
		_putchar(+);
	}
	else if (x = 0)
	{
		_putchar(0);
		_putchar(0);
	}
	else
	{
		_putchar(-1);
		_putchar(-);
	}
	return (0);
}
