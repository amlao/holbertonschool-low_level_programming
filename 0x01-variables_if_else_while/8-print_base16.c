#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main()
{
	char c;
	int i;
	for(i = '0'; i < 10; i++)
	{
		putchar('i');
	}
	for(c = 'a'; c < 'g'; c++)
	{
		putchar('c');
        }
	return(0);
}
